//acc
#include <bits\stdc++.h>
using namespace std;

#define ll long long

int main() {
    ios_base::sync_with_stdio(false); cin.tie(nullptr);

    int M, N; cin >> M; cin >> N;

    ll ans = floor(N*M*0.5);

    cout << ans;

    return 0;
}